This is an explainatory file which is completely intended for understanding manifest.


start_url -> 
    When user starts the page the browser/client loads the url specified in start_url.
    by default it is set to "." which means that the browser will load the root directory of your web app's domain.

    Setting the start_url to "." root directory establishes the entire directory as PWA or Progressive 






